import CrudModule from '@/modules/CrudModule/CrudModule';
import DynamicForm from '@/forms/DynamicForm';
import { fields } from './config';

import useLanguage from '@/locale/useLanguage';

export default function RequestCreationForMaterials() {
  const translate = useLanguage();
  const entity = 'requestcreationformaterials';
  const searchConfig = {
    displayLabels: ['name'],
    searchFields: 'name',
  };
  const deleteModalLabels = ['name'];

  const Labels = {
    PANEL_TITLE: "Request Creation For Materials",
    DATATABLE_TITLE: "Request Creation For Materials",
    ADD_NEW_ENTITY: "New Request Creation For Materials",
    ENTITY_NAME: "Request Creation For Materials",
  };
  const configPage = {
    entity,
    ...Labels,
  };
  const config = {
    ...configPage,
    fields,
    searchConfig,
    deleteModalLabels,
  };
  return (
    <CrudModule
      createForm={<DynamicForm fields={fields} />}
      updateForm={<DynamicForm fields={fields} />}
      config={config}
    />
  );
}
